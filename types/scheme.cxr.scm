(
 (caaaar
   (lambda (pair? pair) *)
   (pure))

 (caaadr
   (lambda (pair? pair) *)
   (pure))

 (caaar
   (lambda (pair? pair) *)
   (pure))

 (caadar
   (lambda (pair? pair) *)
   (pure))

 (caaddr
   (lambda (pair? pair) *)
   (pure))

 (caadr
   (lambda (pair? pair) *)
   (pure))

 (cadaar
   (lambda (pair? pair) *)
   (pure))

 (cadadr
   (lambda (pair? pair) *)
   (pure))

 (cadar
   (lambda (pair? pair) *)
   (pure))

 (caddar
   (lambda (pair? pair) *)
   (pure))

 (cadddr
   (lambda (pair? pair) *)
   (pure))

 (caddr
   (lambda (pair? pair) *)
   (pure))

 (cdaaar
   (lambda (pair? pair) *)
   (pure))

 (cdaadr
   (lambda (pair? pair) *)
   (pure))

 (cdaar
   (lambda (pair? pair) *)
   (pure))

 (cdadar
   (lambda (pair? pair) *)
   (pure))

 (cdaddr
   (lambda (pair? pair) *)
   (pure))

 (cdadr
   (lambda (pair? pair) *)
   (pure))

 (cddaar
   (lambda (pair? pair) *)
   (pure))

 (cddadr
   (lambda (pair? pair) *)
   (pure))

 (cddar
   (lambda (pair? pair) *)
   (pure))

 (cdddar
   (lambda (pair? pair) *)
   (pure))

 (cddddr
   (lambda (pair? pair) *)
   (pure))

 (cdddr
   (lambda (pair? pair) *)
   (pure))
 )
