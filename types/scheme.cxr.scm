(((name . "caaaar") (signature lambda ((pair? pair)) *) (tags pure) (desc . "The (scheme cxr) library exports twenty-four procedures which are the compositions of from three to four car and cdr operations. For example caddar could be defined by (define caddar (lambda (x) (car (cdr (cdr (car x))))))."))
 ((name . "caaadr") (signature lambda ((pair? pair)) *) (tags pure)(desc . "The (scheme cxr) library exports twenty-four procedures which are the compositions of from three to four car and cdr operations. For example caddar could be defined by (define caddar (lambda (x) (car (cdr (cdr (car x))))))."))
 ((name . "caaar") (signature lambda ((pair? pair)) *) (tags pure)(desc . "The (scheme cxr) library exports twenty-four procedures which are the compositions of from three to four car and cdr operations. For example caddar could be defined by (define caddar (lambda (x) (car (cdr (cdr (car x))))))."))
 ((name . "caadar") (signature lambda ((pair? pair)) *) (tags pure)(desc . "The (scheme cxr) library exports twenty-four procedures which are the compositions of from three to four car and cdr operations. For example caddar could be defined by (define caddar (lambda (x) (car (cdr (cdr (car x))))))."))
 ((name . "caaddr") (signature lambda ((pair? pair)) *) (tags pure)(desc . "The (scheme cxr) library exports twenty-four procedures which are the compositions of from three to four car and cdr operations. For example caddar could be defined by (define caddar (lambda (x) (car (cdr (cdr (car x))))))."))
 ((name . "caadr") (signature lambda ((pair? pair)) *) (tags pure)(desc . "The (scheme cxr) library exports twenty-four procedures which are the compositions of from three to four car and cdr operations. For example caddar could be defined by (define caddar (lambda (x) (car (cdr (cdr (car x))))))."))
 ((name . "cadaar") (signature lambda ((pair? pair)) *) (tags pure)(desc . "The (scheme cxr) library exports twenty-four procedures which are the compositions of from three to four car and cdr operations. For example caddar could be defined by (define caddar (lambda (x) (car (cdr (cdr (car x))))))."))
 ((name . "cadadr") (signature lambda ((pair? pair)) *) (tags pure)(desc . "The (scheme cxr) library exports twenty-four procedures which are the compositions of from three to four car and cdr operations. For example caddar could be defined by (define caddar (lambda (x) (car (cdr (cdr (car x))))))."))
 ((name . "cadar") (signature lambda ((pair? pair)) *) (tags pure)(desc . "The (scheme cxr) library exports twenty-four procedures which are the compositions of from three to four car and cdr operations. For example caddar could be defined by (define caddar (lambda (x) (car (cdr (cdr (car x))))))."))
 ((name . "caddar") (signature lambda ((pair? pair)) *) (tags pure)(desc . "The (scheme cxr) library exports twenty-four procedures which are the compositions of from three to four car and cdr operations. For example caddar could be defined by (define caddar (lambda (x) (car (cdr (cdr (car x))))))."))
 ((name . "cadddr") (signature lambda ((pair? pair)) *) (tags pure)(desc . "The (scheme cxr) library exports twenty-four procedures which are the compositions of from three to four car and cdr operations. For example caddar could be defined by (define caddar (lambda (x) (car (cdr (cdr (car x))))))."))
 ((name . "caddr") (signature lambda ((pair? pair)) *) (tags pure)(desc . "The (scheme cxr) library exports twenty-four procedures which are the compositions of from three to four car and cdr operations. For example caddar could be defined by (define caddar (lambda (x) (car (cdr (cdr (car x))))))."))
 ((name . "cdaaar") (signature lambda ((pair? pair)) *) (tags pure)(desc . "The (scheme cxr) library exports twenty-four procedures which are the compositions of from three to four car and cdr operations. For example caddar could be defined by (define caddar (lambda (x) (car (cdr (cdr (car x))))))."))
 ((name . "cdaadr") (signature lambda ((pair? pair)) *) (tags pure)(desc . "The (scheme cxr) library exports twenty-four procedures which are the compositions of from three to four car and cdr operations. For example caddar could be defined by (define caddar (lambda (x) (car (cdr (cdr (car x))))))."))
 ((name . "cdaar") (signature lambda ((pair? pair)) *) (tags pure)(desc . "The (scheme cxr) library exports twenty-four procedures which are the compositions of from three to four car and cdr operations. For example caddar could be defined by (define caddar (lambda (x) (car (cdr (cdr (car x))))))."))
 ((name . "cdadar") (signature lambda ((pair? pair)) *) (tags pure)(desc . "The (scheme cxr) library exports twenty-four procedures which are the compositions of from three to four car and cdr operations. For example caddar could be defined by (define caddar (lambda (x) (car (cdr (cdr (car x))))))."))
 ((name . "cdaddr") (signature lambda ((pair? pair)) *) (tags pure)(desc . "The (scheme cxr) library exports twenty-four procedures which are the compositions of from three to four car and cdr operations. For example caddar could be defined by (define caddar (lambda (x) (car (cdr (cdr (car x))))))."))
 ((name . "cdadr") (signature lambda ((pair? pair)) *) (tags pure)(desc . "The (scheme cxr) library exports twenty-four procedures which are the compositions of from three to four car and cdr operations. For example caddar could be defined by (define caddar (lambda (x) (car (cdr (cdr (car x))))))."))
 ((name . "cddaar") (signature lambda ((pair? pair)) *) (tags pure)(desc . "The (scheme cxr) library exports twenty-four procedures which are the compositions of from three to four car and cdr operations. For example caddar could be defined by (define caddar (lambda (x) (car (cdr (cdr (car x))))))."))
 ((name . "cddadr") (signature lambda ((pair? pair)) *) (tags pure)(desc . "The (scheme cxr) library exports twenty-four procedures which are the compositions of from three to four car and cdr operations. For example caddar could be defined by (define caddar (lambda (x) (car (cdr (cdr (car x))))))."))
 ((name . "cddar") (signature lambda ((pair? pair)) *) (tags pure)(desc . "The (scheme cxr) library exports twenty-four procedures which are the compositions of from three to four car and cdr operations. For example caddar could be defined by (define caddar (lambda (x) (car (cdr (cdr (car x))))))."))
 ((name . "cdddar") (signature lambda ((pair? pair)) *) (tags pure)(desc . "The (scheme cxr) library exports twenty-four procedures which are the compositions of from three to four car and cdr operations. For example caddar could be defined by (define caddar (lambda (x) (car (cdr (cdr (car x))))))."))
 ((name . "cddddr") (signature lambda ((pair? pair)) *) (tags pure)(desc . "The (scheme cxr) library exports twenty-four procedures which are the compositions of from three to four car and cdr operations. For example caddar could be defined by (define caddar (lambda (x) (car (cdr (cdr (car x))))))."))
 ((name . "cdddr") (signature lambda ((pair? pair)) *) (tags pure)(desc . "The (scheme cxr) library exports twenty-four procedures which are the compositions of from three to four car and cdr operations. For example caddar could be defined by (define caddar (lambda (x) (car (cdr (cdr (car x)))))).")))
