(((name . "rpair?") (signature lambda (obj) boolean?) (tags pure predicate) (desc . "Returns #t if obj is a pair, and otherwise returns #f."))
 ((name . "rcons") (signature lambda (obj1 obj2) rpair?) (tags pure) (desc . "Returns a newly allocated pair whose car is obj1 and whose cdr is obj2. The pair is guaranteed to be different (in the sense of eqv?) from every existing object. This operation must take O(1) time."))
 ((name . "rcar") (signature lambda ((rpair? pair)) *) (tags pure) (desc . "Returns the contents of the car field of pair. This operation must take O(1) time."))
 ((name . "rcdr") (signature lambda ((rpair? pair)) *) (tags pure) (desc . "Returns the contents of the cdr field of pair. This operation must take O(1) time."))
 ((group
    ((name . "rcaar") (signature lambda ((rpair? pair)) *) (tags pure))
    ((name . "rcadr") (signature lambda ((rpair? pair)) *) (tags pure))
    ((name . "rcdar") (signature lambda ((rpair? pair)) *) (tags pure))
    ((name . "rcddr") (signature lambda ((rpair? pair)) *) (tags pure))
    ((name . "rcaaar") (signature lambda ((rpair? pair)) *) (tags pure))
    ((name . "rcaadr") (signature lambda ((rpair? pair)) *) (tags pure))
    ((name . "rcadar") (signature lambda ((rpair? pair)) *) (tags pure))
    ((name . "rcaddr") (signature lambda ((rpair? pair)) *) (tags pure))
    ((name . "rcdaar") (signature lambda ((rpair? pair)) *) (tags pure))
    ((name . "rcdadr") (signature lambda ((rpair? pair)) *) (tags pure))
    ((name . "rcddar") (signature lambda ((rpair? pair)) *) (tags pure))
    ((name . "rcdddr") (signature lambda ((rpair? pair)) *) (tags pure))
    ((name . "rcaaaar") (signature lambda ((rpair? pair)) *) (tags pure))
    ((name . "rcaaadr") (signature lambda ((rpair? pair)) *) (tags pure))
    ((name . "rcaadar") (signature lambda ((rpair? pair)) *) (tags pure))
    ((name . "rcaaddr") (signature lambda ((rpair? pair)) *) (tags pure))
    ((name . "rcadaar") (signature lambda ((rpair? pair)) *) (tags pure))
    ((name . "rcadadr") (signature lambda ((rpair? pair)) *) (tags pure))
    ((name . "rcaddar") (signature lambda ((rpair? pair)) *) (tags pure))
    ((name . "rcadddr") (signature lambda ((rpair? pair)) *) (tags pure))
    ((name . "rcdaaar") (signature lambda ((rpair? pair)) *) (tags pure))
    ((name . "rcdaadr") (signature lambda ((rpair? pair)) *) (tags pure))
    ((name . "rcdadar") (signature lambda ((rpair? pair)) *) (tags pure))
    ((name . "rcdaddr") (signature lambda ((rpair? pair)) *) (tags pure))
    ((name . "rcddaar") (signature lambda ((rpair? pair)) *) (tags pure))
    ((name . "rcddadr") (signature lambda ((rpair? pair)) *) (tags pure))
    ((name . "rcdddar") (signature lambda ((rpair? pair)) *) (tags pure))
    ((name . "rcddddr") (signature lambda ((rpair? pair)) *) (tags pure)))
  (desc . "These procedures are compositions of car and cdr."))
 ((name . "rnull?") (signature lambda (obj) boolean?) (tags pure) (desc . "Returns #t if obj is the empty list, #f otherwise."))
 ((name . "rlist?")
  (signature lambda (obj) boolean?)
  (tags pure predicate)
  (desc . "Returns #t if obj is a list, #f otherwise. By definition, all lists are chains of pairs that have finite length and are terminated by the empty list. This operation must take time bounded by O(log(n)), where n is the number of pairs in the chain forming the potential list."))
 ((name . "rlist") (signature lambda (obj ...) rlist?) (tags pure) (desc . "Returns a newly allocated list of its arguments. This operation must take time bounded by O(n), where n is the number of arguments to list."))
 ((name . "make-rlist")
  (signature case-lambda (((integer? k)) rlist?) (((integer? k) obj) rlist?))
  (tags pure)
  (desc . "Returns a newly allocated list of k elements. If a second argument is given, then each element is initialized to obj. Otherwise the initial contents of each element is unspecified. This operation must take time and space bounded by O(log(k))."))
 ((name . "rlength") (signature lambda ((rlist? list)) integer?) (tags pure) (desc . "Returns the length of list. This operation must take time bounded by O(log(n)), where n is the length of the list."))
 ((name . "rlength<=?")
  (signature lambda (obj (integer? k)) boolean?)
  (tags pure)
  (desc . "Returns true if obj is a chain of at least k pairs and false otherwise. This operation must take time bounded by O(log(min(k,n))), where n is the length of the chain of pairs."))
 ((name . "rappend")
  (signature
   case-lambda
   (((rlist? list) ...) rlist?)
   (((rlist? list) ... obj) *))
  (tags pure)
  (desc . "Returns a chain of pairs consisting of the elements of the first list followed by the elements of the other lists, with obj as the cdr of the final pair. An improper list results if obj is not a list. This operation must take time bounded by O(log(n)), where n is the total number of elements in the given lists."))
 ((name . "rreverse") (signature lambda ((rlist? list)) rlist?) (tags pure) (desc . "Returns a newly allocated list consisting of the element of list in reverse order. This operation must take time bounded by O(n) where n is the length of the list."))
 ((name . "rlist-tail")
  (signature
   case-lambda
   (((rlist? list) (integer? k)) rlist?)
   ((obj (integer? k)) *))
  (tags pure)
  (desc . "Obj should be a chain of pairs with a count of at least k. The list-tail procedure returns the object obtained by omitting the first k elements in obj. This operation must take time bounded by O(log(min(k,n))), where n is the length of the chain of pairs."))
 ((name . "rlist-ref")
  (signature lambda ((rpair? pair) (integer? k)) *)
  (tags pure)
  (desc . "Pair must be a chain of pairs whose count is at least k + 1. The list-ref procedure returns the kth element of pair. This operation must take time bounded by O(min(k,log(n))), where n is the length of the chain of pairs."))
 ((name . "rlist-set")
  (signature lambda ((rpair? pair) (integer? k) obj) rpair?)
  (tags pure)
  (desc . "Pair must be a chain of pairs whose count is at least k + 1. The list-set procedure returns the chain of pairs obtained by replacing the kth element with obj. This operation must take time bounded by O(min(k,log(n))), where n is the length of the chain of pairs."))
 ((name . "rlist-ref/update")
  (signature
   lambda
   ((rpair? pair) (integer? k) (procedure? proc))
   (values * rpair?))
  (subsigs (proc (lambda (obj) *)))
  (tags pure)
  (desc . "Returns the same results as:
(values (list-ref pair k) 
        (list-set pair k (proc (list-ref pair k))))

but it may be implemented more efficiently."))
 ((name . "rmap")
  (signature
   lambda
   ((procedure? proc) (rlist? list1) (rlist? list2) ...)
   rlist?)
  (subsigs (proc (lambda (obj1 obj2 ...) *)))
  (tags pure)
  (desc . "The lists should all have the same length. Proc should accept as many arguments as there are lists and return a single value.
The map procedure applies proc element-wise to the elements of the lists and returns a list of the results, in order. Proc is always called in the same dynamic environment as map itself. The order in which proc is applied to the elements of the lists is unspecified. "))
 ((name . "rfor-each")
  (signature
   lambda
   ((procedure? proc) (rlist? list1) (rlist? list2) ...)
   undefined)
  (subsigs (proc (lambda (obj1 obj2 ...) undefined)))
  (tags pure)
  (desc . "The lists should all have the same length. Proc should accept as many arguments as there are lists.
The for-each procedure applies proc element-wise to the elements of the lists for its side effects, in order from the first element to the last. Proc is always called in the same dynamic environment as for-each itself. The return values of for-each are unspecified."))
 ((group
    ((name . "rlist->list") (signature lambda ((rlist? rlist)) list?) (tags pure))
    ((name . "list->rlist") (signature lambda ((list? list)) rlist?) (tags pure)))
  (desc . "These procedures convert between (potentially) distinct representations of lists. To avoid confusion, parameters named ra-list range over lists represented with random-access lists, i.e. objects satisfying the list? predicate described above, while parameters named la-list range over lists represented with the more traditional linear-access lists, i.e. objects satisfying the list? predicate of R6RS. In systems that represent all lists as random-access lists, these conversions may simply be list identity procedures.")))
