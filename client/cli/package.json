{
  "name": "client",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start": "tsc && node out/scmindex.js --url http://localhost:8080",
    "help": "tsc && node out/scmindex.js --help",
    "non-interactive": "tsc && node out/scmindex.js --filterset bigloo --query map --strict --url http://localhost:8080",
    "build": "tsc && pkg --debug out/scmindex.js --targets latest-win-x64,latest-linuxstatic-x64 --out-path dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^13.13.4",
    "pkg": "^5.8.0",
    "typescript": "^4.8.4"
  },
  "dependencies": {
    "ts-command-line-args": "^2.3.1",
    "typed-rest-client": "^1.8.9",
    "scmindex-common": "file:../common"
  }
}
