import { FacetFilterPipe } from './facet-filter.pipe';

describe('FacetFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new FacetFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
